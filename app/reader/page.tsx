'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';
const chapters=[['The Moon Garden','The moon had never looked this close.'],['A Stranger in Starlight','The garden changed when the stranger stepped beneath the silver trees.'],['The Forgotten Promise','A memory surfaced beneath a sky full of ancient constellations.']];
export default function Reader(){const [size,setSize]=useState(false);const [liked,setLiked]=useState(false);const [chapter,setChapter]=useState(0);useEffect(()=>{localStorage.setItem('elaria-reading',String(chapter+1))},[chapter]);return <main className={'reading '+(size?'large':'')}><header className="readerNav"><Link href="/novel/1">← Moonlit Promise</Link><strong>CHAPTER {String(chapter+1).padStart(2,'0')}</strong><div><button onClick={()=>setSize(!size)}>Aa</button><button onClick={()=>setLiked(!liked)}>{liked?'♥':'♡'}</button></div></header><article className="chapter"><small>THE ELARIA ARCHIVES · CHAPTER {chapter+1}</small><h1>{chapters[chapter][0].split(' ')[0]} <i>{chapters[chapter][0].split(' ').slice(1).join(' ')}</i></h1><p className="lead">{chapters[chapter][1]}</p><p>Lyra stood beneath the silver trees, watching pale light spill across the quiet garden. Every flower seemed to breathe with the night, opening only when the stars appeared.</p><p>Then she heard footsteps behind her.</p><p>“You came.”</p><p>She turned. A stranger stood beyond the archway, his dark coat touched by moonlight. For a moment neither of them spoke. Somewhere above, a constellation shifted like a secret being written across the sky.</p><p>“I promised I would,” he said softly.</p><p>Lyra felt her heart skip. She did not remember making a promise to him.</p><div className="chapterQuote">✦<br/><i>Some promises remember us,<br/>even when we forget them.</i></div><div className="readerEnd"><span>END OF CHAPTER {chapter+1}</span><div><Link href="/novel/1">Chapter list</Link>{chapter<chapters.length-1&&<button className="primary" onClick={()=>setChapter(chapter+1)}>Next chapter →</button>}</div></div></article></main>}
