'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';
const saved=['Moonlit Promise','Crimson Eclipse','Starlight Academy'];
export default function Library(){const [tab,setTab]=useState('favorites');const [progress,setProgress]=useState('1');useEffect(()=>setProgress(localStorage.getItem('elaria-reading')||'1'),[]);return <main className="fantasy"><header className="navWrap"><nav className="nav"><Link className="logo" href="/">✦ ELARIA</Link><div className="navLinks"><Link href="/">Home</Link><Link href="/explore">Explore</Link><Link href="/genres">Genres</Link><Link href="/rankings">Rankings</Link><Link className="active" href="/library">Library</Link></div><Link className="sign" href="/search">Search</Link></nav></header><section className="pageHero"><small className="label">YOUR PERSONAL SHELF</small><h1>Your <i>library.</i></h1><p>Keep the worlds you love close.</p></section><section className="section"><div className="libraryTabs"><button className={tab==='favorites'?'selected':''} onClick={()=>setTab('favorites')}>♡ Favorites</button><button className={tab==='history'?'selected':''} onClick={()=>setTab('history')}>◷ Reading history</button><button className={tab==='following'?'selected':''} onClick={()=>setTab('following')}>✦ Following</button></div>{tab==='history'?<div className="emptyLibrary"><div>◷</div><h2>Your reading journey.</h2><p>You last visited chapter {progress}. Continue your story whenever you're ready.</p><Link className="primary" href="/reader">Continue reading →</Link></div>:tab==='following'?<div className="emptyLibrary"><div>✦</div><h2>Nothing followed yet.</h2><p>Discover a world and follow it to keep it close.</p><Link className="primary" href="/explore">Explore worlds →</Link></div>:<div className="cards">{saved.map((x,i)=><article key={x}><Link href={`/novel/${i+1}`}><div className={'cover c'+i}><span>✦ 0{i+1}</span><b>{['♡','☾','✧'][i]}</b></div></Link><small>FAVORITE · FANTASY</small><h3>{x}</h3><p>Continue your journey from where you left off.</p><Link className="enter" href="/reader">Continue reading →</Link></article>)}</div>}</section></main>}
